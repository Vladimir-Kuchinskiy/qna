h2 Question page

h4 Title: #{@question.title}
h4 Body:  #{@question.body}

h4 Answers:

- @question.answers.each do |answer|
  p = answer.body

p = link_to 'Add New Answer', new_question_answer_path(@question)

p = link_to 'Back', questions_path
