h2 Question page

h4 Title: #{@question.title}
h4 Body:  #{@question.body}

h4 Answers:

- @question.answers.each do |answer|
  p data-answer="#{answer.id}" = answer.body

h4 Add New Answer:
= render 'answers/form', question: @question, answer: @question.answers.new

p = link_to 'Back', questions_path
